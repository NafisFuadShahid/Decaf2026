# Fed-CRC-Seg dependencies (DeCaF 2026)
# Versions used in the paper experiments (Python 3.11, CUDA 11.8)

# Deep learning + medical imaging
torch==2.7.1+cu118
monai==1.5.2
nibabel==5.4.2

# Numerical / scientific
numpy==2.1.3
scipy==1.15.3
pandas==2.2.3

# Visualization
matplotlib==3.10.0

# Data access (FeTS-2022 requires a Synapse account + DUA acceptance)
synapseclient==4.12.0

# Installation notes:
# 1. Install PyTorch with CUDA 11.8:
#    pip install torch==2.7.1+cu118 --index-url https://download.pytorch.org/whl/cu118
# 2. Install the rest:
#    pip install monai nibabel numpy scipy pandas matplotlib synapseclient
# 3. CPU-only (inference will be ~10-20x slower):
#    pip install torch==2.7.1 --index-url https://download.pytorch.org/whl/cpu
